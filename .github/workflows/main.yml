name: Deploy to EC2

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2


      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18 # Use the desired Node.js version

      - name: Install dependencies
        run: npm install
        working-directory: fe/

      - name: Build frontend
        run: npm run bundleprod
        working-directory: fe/
        


 
      - name: finding the path 
        run: which sshpass
      - name: SSH into EC2
        run: |
          echo 'Dev0981' | sshpass -p -e ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -v ubuntu@13.126.77.241 "ls -la"

 
          
      
   
